@model AkwadratDesign.Models.DbModels.ProjectFirm

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ProjectFirm</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="FirmsId" class="control-label"></label>
                <select asp-for="FirmsId" class ="form-control" asp-items="ViewBag.FirmsId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ProjectsId" class="control-label"></label>
                <select asp-for="ProjectsId" class ="form-control" asp-items="ViewBag.ProjectsId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
